<odoo>

    <record id="visit_visit_view_tree" model="ir.ui.view">
        <field name="name">visit.visit.tree</field>
        <field name="model">visit.visit</field>
        <field name="arch" type="xml">
            <tree string="Visits">
                <field name="contract_line_id"/>
                <field name="contract_id"/>
                <field name="so_due_date"/>
                <field name="recurrence_id"/>
                <field name="visit_type"/>
                <field name="hours"/>
                <field name="unit_price"/>
                <field name="actual_hrs"/>
                <field name="sale_id"/>
                <field name="fsm_order_id"/>
                <field name="fso_status"/>
                <field name="fso_schedule_date"/>
            </tree>
        </field>
    </record>

    <record id="visit_visit_view_form" model="ir.ui.view">
        <field name="name">visit.visit.form</field>
        <field name="model">visit.visit</field>
        <field name="arch" type="xml">
            <form string="Visits">
                <sheet>
                    <group col="4">
                        <field name="contract_line_id"/>
                        <field name="contract_id"/>
                        <field name="recurrence_id"/>
                        <field name="visit_type"/>
                        <field name="hours"/>
                        <field name="unit_price"/>
                        <field name="actual_hrs"/>
                        <field name="so_due_date"/>
                        <field name="sale_id"/>
                        <field name="fsm_order_id"/>
                        <field name="fso_status"/>
                        <field name="fso_schedule_date"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="visit_visit_view_search" model="ir.ui.view">
        <field name="name">visit.visit.search</field>
        <field name="model">visit.visit</field>
        <field name="arch" type="xml">
            <search string="Visits">
                <field name="contract_line_id"/>
                <field name="contract_id"/>
                <field name="recurrence_id"/>
                <field name="visit_type"/>
                <field name="so_due_date"/>
                <field name="sale_id"/>
                <field name="fsm_order_id"/>
                <field name="fso_status"/>
                <field name="fso_schedule_date"/>
                <separator/>
                <group expand="0" string="Group By...">
                    <filter string="Contract"
                            name="contract_id"
                            context="{'group_by':'contract_id'}"/>
                    <filter string="Recurrence"
                            name="recurrence_id"
                            context="{'group_by':'recurrence_id'}"/>
                    <filter string="Visit Type"
                            name="visit_type"
                            context="{'group_by':'visit_type'}"/>
                    <filter string="SO Number"
                            name="sale_id"
                            context="{'group_by':'sale_id'}"/>
                    <filter string="FSO"
                            name="fsm_order_id"
                            context="{'group_by':'fsm_order_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_visit_visit_form" model="ir.actions.act_window">
        <field name="name">Visits</field>
        <field name="res_model">visit.visit</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="visit_visit_view_tree"/>
        <field name="search_view_id" ref="visit_visit_view_search"/>
    </record>

    <menuitem id="menu_visit_visit"
              name="Visits"
              sequence="3"
              action="action_visit_visit_form"
              parent="sale.sale_menu_root"
    />

</odoo>
