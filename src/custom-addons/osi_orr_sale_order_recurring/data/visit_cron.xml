<odoo>

	<record id="contract_recurring_cron_for_sale" model="ir.cron">
        <field name="name">Recurring SO Generation from Contract</field>
        <field name="model_id" ref="model_visit_visit"/>
        <field name="state">code</field>
        <field name="code">model.cron_contract_recurring_create_sale()</field>
        <field name="user_id" ref="base.user_root" />
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field eval="False" name="doall" />
    </record>

</odoo>
